steps:
  - uses: actions/checkout@v4
  - uses: actions/setup-node@v4
    with:
      node-version: '20'
  - name: Install Dependencies
    run: npm ci
  - name: Initialize Database
    run: |
      npm run db:generate
      npm run db:push